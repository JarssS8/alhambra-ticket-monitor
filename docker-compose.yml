version: '3.8'

services:
  alhambra-monitor:
    build: .
    container_name: alhambra-monitor
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
